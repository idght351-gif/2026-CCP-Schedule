{
  "entities": {
    "Schedule": {
      "title": "Schedule",
      "description": "운영 일정 데이터",
      "type": "object",
      "properties": {
        "id": { "type": "number" },
        "teamName": { "type": "string" },
        "leaderName": { "type": "string" },
        "phone": { "type": "string" },
        "date": { "type": "string" },
        "location": { "type": "string" },
        "keyword": { "type": "string" },
        "count": { "type": "number" },
        "memo": { "type": "string" }
      },
      "required": ["teamName", "leaderName", "date", "location", "keyword", "count"]
    },
    "ChangeRequest": {
      "title": "ChangeRequest",
      "description": "차수 변경 요청 데이터",
      "type": "object",
      "properties": {
        "id": { "type": "number" },
        "our": { "$ref": "Schedule" },
        "target": { "$ref": "Schedule" },
        "status": { "type": "string", "enum": ["pending", "approved", "rejected"] },
        "timestamp": { "type": "string" },
        "sentSmsOur": { "type": "boolean" },
        "sentSmsTarget": { "type": "boolean" }
      },
      "required": ["our", "target", "status", "timestamp"]
    },
    "AdminLog": {
      "title": "AdminLog",
      "description": "관리자 작업 로그",
      "type": "object",
      "properties": {
        "adminKey": { "type": "string" },
        "action": { "type": "string" },
        "targetId": { "type": "string" },
        "details": { "type": "string" },
        "timestamp": { "type": "string" }
      },
      "required": ["adminKey", "action", "timestamp"]
    }
  },
  "firestore": {
    "/schedules/{scheduleId}": {
      "schema": "Schedule",
      "description": "운영 일정 컬렉션"
    },
    "/changeRequests/{requestId}": {
      "schema": "ChangeRequest",
      "description": "차수 변경 요청 컬렉션"
    },
    "/logs/{logId}": {
      "schema": "AdminLog",
      "description": "관리 감사 로그 컬렉션"
    }
  }
}
